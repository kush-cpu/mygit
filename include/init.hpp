// init.hpp
#ifndef MYGIT_INIT_HPP
#define MYGIT_INIT_HPP

#include <string>

// Declares the init function so it can be used in main.cpp
void init_repository(const std::string& path);

#endif // MYGIT_INIT_HPP
